# Plan: Create new file called HEJ.md

Issue: #19

## Context
A new file called `HEJ.md` needs to be created in the repository root. The repository is currently empty (no existing files), so this is a straightforward file addition.

## Approach
Create a new Markdown file named `HEJ.md` in the repository root. Since the issue does not specify any content for the file, it should be created with a minimal, sensible default (e.g., a heading).

## Changes

### 1. Create `HEJ.md`
**Files:** `HEJ.md` (new file)

Create a new file `HEJ.md` in the repository root. Since no content is specified in the issue, the file should contain at minimum a heading. "Hej" is Swedish for "Hello", so a simple greeting heading would be appropriate.

## Open questions
1. **What content should `HEJ.md` contain?** The issue only specifies creating the file but does not describe its contents. Should it be empty, contain a greeting, or have specific content?

## Risks
- None. This is a new file addition with no impact on existing code or functionality.

## Testing
- Verify that `HEJ.md` exists in the repository root after the change.
- Verify the file contains valid Markdown.
